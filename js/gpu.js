"use strict"

let gpu = {

}

function draw() {
    let canvas = document.querySelector("#game-screen")
    let context = canvas.getContext("2D")
    context.moveTo(0, 0);
    context.lineTo(200, 100);
    ctx.stroke();
}